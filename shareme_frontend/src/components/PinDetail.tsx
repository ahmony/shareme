import React from 'react'
import { FetchedUser } from '../types/sanityInterfaces'

const PinDetail = (props: { user: FetchedUser }) =>
{
    return (
        <div>PinDetail</div>
    )
}

export default PinDetail